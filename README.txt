These are some MATLAB files to use to get true stress and true strain from images and load data. 

Use OutputFigure.m as your main file, the other three are called from there. 

Branch to change the primary use case to tensile experiment using an instron testing rig. 